#!/usr/bin/env ruby
# frozen_string_literal: true

# realpath so a symlinked bin/dev-env still finds lib/ next to the source.
require File.expand_path("../lib/dev_env", File.dirname(File.realpath(__FILE__)))

DevEnv::CLI.new.start(ARGV)
